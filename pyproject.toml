[project]
name = "dotfiles"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["rich>=13.0"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["scripts"]
addopts = "--ignore=scripts/test_distrobox_integration.py"
